# VoxEngine 需求记录

## 002 · C++ 连续发音（JSON 数组输入）
**状态**: 实现中
1. 输入 JSON 数组 `[{"phoneme":"a","duration":0.5,"f0":180}, {"phoneme":"i","duration":0.5,"f0":200}]`
2. 循环遍历，按每个音素自己的 duration 和 f0 调用现有单音素渲染
3. 顺序拼接成一个完整 Float32Array
4. **核心防爆音**：每段首尾各 15ms 线性淡入淡出
5. 拼接后导出 `tui_seq.wav`
**约束**:
- 音素间允许生硬切换，不做滑音（后期靠 JS 补丁润色）
- 避免频繁内存重分配：先算总采样数，一次性分配

## 003 · 内存直出，WAV 不做按钮
**状态**: 实现中
- 核心接口返回**内存缓冲**（Float32Array/float 数组）供共享直出，不把 WAV 当主要产出
- **不额外做「输出 WAV」按钮**；写文件只是可选的旁路，不是 UI 动作

## 004 · 可替换引擎 + 插件 + 布局文件外置
**状态**: 待办（记录）

### A. 可替换引擎
- 抽象出引擎接口，DSP 后端可换（当前是 JNI → libvox.so）
- 候选实现：NativeEngine（现有）/ CpuEngine（纯 Java 兜底）/ OnnxEngine（后期）/ 远端引擎
- 运行时可选择与切换，不重新编译 APK

### B. 插件
- 插件 = 一个目录/zip，含 `manifest.json` + 可选资源
- 可挂载的插件类型：
  - `phones.json` —— 音素表（多语言）
  - `layout.xml` —— 界面布局
  - `engine.so` —— 原生引擎（dlopen）
  - `patch.js` —— 后期 JS 润色补丁
- Android 10+ 限制：**外部存储的 dex 不能动态加载**，所以插件走「数据 + 原生库」，
  不走「外置 dex」

### C. 布局文件外置（可热改）
- 界面布局从 `res/layout/`（编译期固化）改为**运行时读文件 + inflate**
- 改布局只需替换文件 + 重新 inflate，**不用重装 APK**

## 005 · ONNX 主动降噪 + 润色模块
**状态**: 待办（记录）
**要求**:
1. ONNX 推理模块：对 DSP 骨架做**主动降噪 + 润色**
2. **异步加载**：模型加载不阻塞界面/音频线程
3. **与声码器异步输出**：声码器（DSP 引擎）出帧 → 推理线程消费 → 流式输出，不互相等待
4. 消耗尽量少（内存/CPU/包体）
**已知约束**:
- Termux 里 onnxruntime-node 无 android/arm64 预编译包 → 走 Android AAR
- Android 10+ 不能加载外置 dex → ONNX 必须打进 APK

## 005 补充 · 优化目标改成「运行时内存」而非包体
**包体大小无所谓**（+6.5MB 或 +18MB 都可以接受）
**真正要控的是运行时内存占用**，措施：

| 措施 | 目的 |
|---|---|
| 模型按**文件路径**加载，不进 Java 堆 | 省掉一份完整模型副本 |
| `setEnableCpuMemArena(false)` | 关掉 ORT 内存池，压峰值 |
| `setEnableMemPattern(false)` | 关掉内存复用图 |
| intra/inter 线程数 = 1 | 线程栈与临时缓冲最小 |
| 小帧处理（512~1024 采样） | 张量恒定几 KB，不随总时长增长 |
| 张量/结果 **try-with-resources 立即释放** | 不让 native 内存滞留到 GC |
| 复用 float[] 缓冲，零分配循环 | 避免 GC 抖动 |
| 有界队列（1~2 帧） | 异步但不堆积 |
| 异步加载，就绪前**透传** | 不额外缓冲、不阻塞 |

## 006 · 三个模型的插件槽位（后期训练，先留接口）
**状态**: 记录

| 槽位 | 用途 | 备注 |
|---|---|---|
| `polish.onnx` | 通用润色 | 对所有输出做音质润色 |
| `speech.onnx` | 说话模型 | 含 F0 预测 |
| `singing.onnx` | 歌唱模型 | 含**基频预测与 F0 预测**（歌唱专用） |

**放在 app 私有目录**（插件架构，不打进 APK）：
```
files/models/polish.onnx
files/models/speech.onnx
files/models/singing.onnx
```

**内存策略**：
- **同一时刻只加载一个模型**，切换模式时卸载旧的（峰值内存 = 单模型 + ORT session）
- 输入/输出形状**从 session 元数据读取**，不写死 —— 三个模型形状不同也能跑
- 量化模型（Q8/INT8）内存约为 FP32 的 1/4，优先用

**待确认**：模型的量化格式
- ONNX + Q8（int8）→ ONNX Runtime 直接跑 ✅
- **GGUF** → ONNX Runtime **不支持**，需要 llama.cpp 后端 ❗ 两条路不通

## 006 定稿 · 三个模型都用 ONNX
| 槽位 | 格式 | 量化 | 用途 |
|---|---|---|---|
| `polish.onnx` | ONNX | **Q4** | 通用润色 |
| `speech_f0.onnx` | ONNX | **Q8** | 说话：F0 预测 |
| `singing_f0.onnx` | ONNX | **Q8** | 歌唱：基频 + F0 预测 |
| 声码器 | —— | —— | **经典款**（现有 DSP 引擎，不做神经声码器） |

**后端**：ONNX Runtime（arm64-v8a，已验证依赖干净）
**内存**：同一时刻只加载一个模型；Q8 约为 FP32 的 1/4，Q4 约 1/8

## 007 · 内置无训练音色
**状态**: 记录
**结论**: 可以。经典共振峰合成本身就是**无训练**的 —— 音色由参数决定，不靠模型。

**音色 = 一组参数**（数据文件，可热改，符合 004 插件架构）：
```
voices/default.json
voices/male.json     voices/female.json     voices/child.json
```
参数内容：
- 声门源：LF 参数（Tp/Te/Ta/α）、开商、张力
- 声道：F1~F5 基准 + 带宽、声线缩放
- 质感：jitter / shimmer / 气息感 / 鼻音耦合
- 韵律：基频范围、颤音深度与速率、滑音时长

**内置默认歌手（纯参数合成，不需要 zip、不需要模型）**：
- **一个男声、一个女生**
- 新轨道默认无歌手；点头像可切到这两个内置歌手，或加载 zip 声库

**立绘**：待定（作者暂不擅绘画）
- 先用**程序生成的占位头像**（色块 + 首字）
- **格式用 WebP**（不是 PNG）：体积更小、支持透明通道
- 字段先留好，后续可换真实立绘

**待修（已诊断但未修）**：
- LF 的 α 现在是 `1/(2·Ta)` → **α·T0 = 25**，自然嗓音是 2~5。
  开相脉冲上升 6300 倍 = 近似冲击 → 声音发尖发毛躁。这是音色"电子味"的主因之一。

## 008 · JS 插件接口（外挂 JS 即插件）
**状态**: 记录

JS 插件文件就是插件本体，运行时被加载，**由宿主主动喂入真实数据**。

### 宿主 → 插件（每帧/每段暴露）
| 字段 | 含义 |
|---|---|
| `spectrum` | **当前真实频谱**（DSP 实际输出，FFT 幅度谱） |
| `f0Spectrum` | **F0 预测频谱**（预测模型给出的谱） |
| `f0Contour` | 基频曲线（逐帧 F0） |
| `duration` | 输出总时长（秒） |
| `sampleRate` / `frameSize` / `frameIndex` | 常规音频参数 |
| `phonemes` | 当前音素序列（符号/时长/F0） |
| `rms` / `peak` | 电平 |
| `voice` | 当前音色参数表 |

### 插件 → 宿主（钩子）
| 钩子 | 作用 |
|---|---|
| `onInit(ctx)` | 初始化，拿到采样率/帧长/音色表 |
| `onF0(data)` | 改基频曲线（滑音、颤音、音准修正） |
| `onSpectrum(data)` | 改频谱（均衡、共振峰微调、去齿音） |
| `onFrame(data)` | 改时域帧（润色、降噪残余） |
| `onFinish(stats)` | 收尾，拿到统计 |

### 约束
- 每个钩子都是**可选**的；缺失即跳过，不报错
- 返回 `null` / `undefined` = 不改动
- **未加载插件 → 完全直通**（零开销）
- 插件异常被捕获并计数，连续失败则自动停用（不拖垮音频线程）

### JS 运行时（待选，不用 WebView）
| 方案 | 体积 | 说明 |
|---|---|---|
| **QuickJS** | ~500KB | C 实现，需纳入 native 构建 |
| Rhino | ~1.2MB | 纯 Java，直接进 dex，无需编 native |

## 008 定稿 · JS 运行时选 QuickJS
**理由**: 完整 ES2020（async/await、解构、类），体积 970KB，无需 npm 生态
| 项 | 结果 |
|---|---|
| 编译 | ✅ `libquickjs.so` **970KB**，依赖仅 libm/libdl/libc |
| 插件加载 | ✅ `JS_Eval` 加载 plugin.js，检测钩子是否存在 |
| 数据往返 | ✅ 宿主建对象 → 插件改 → 读回（f0×1.02、频谱去齿音实测通过） |
| 内存上限 | ✅ `JS_SetMemoryLimit(rt, 4MB)`，插件无法吃爆内存 |
| 泄漏 | ✅ 钩子函数句柄需显式释放，否则 `JS_FreeRuntime` 断言失败 |

**踩坑记录**：首编未显式 `-lm`，`libquickjs.so` 引用了 `pow`/`fesetround` 但 NEEDED 里没有 libm
→ Android 上会 "cannot locate symbol" 崩溃。**打包前必须查 NEEDED**。

## 006 修订 · 三个模型统一 Q8
| 槽位 | 量化 |
|---|---|
| `polish.onnx` | **Q8** |
| `speech_f0.onnx` | **Q8** |
| `singing_f0.onnx` | **Q8** |

原 Q4（仅 MatMulNBits 支持，Conv/GRU 结构量化不出来）取消。
统一 Q8 = INT8，内存约为 FP32 的 **1/4**，ONNX Runtime 标准量化工具直接出。

## 009 · 钢琴卷帘 UI（借鉴 OpenUtau）
**状态**: 仅记录，不实现

**许可**: OpenUtau 是 **MIT**（© 2014 StAkira）
- ✅ 可借鉴/复用其 UI 布局与交互模型
- ✅ MIT 允许直接复制代码（但它是 C#/Avalonia，我们需用 Java/Canvas 重写）
- ⚠️ 不抄其图标/位图资源（第三方图标集许可各异）——自己画或换开源图标
- ⚠️ 不碰 worldline/声库等捆绑组件（NixOS 专门讨论过其许可复杂度）
- 需在「关于」页署名：`UI inspired by OpenUtau (MIT, © 2014 StAkira)`

**要照搬的交互模型**：
```
音轨列表  | [歌手/音色] [音量/声像]
──────────┼──────────────────────────────
钢琴卷帘  | 音符块：拖拽/拉伸/调音高；显示歌词→音素映射
──────────┼──────────────────────────────
音高轨道  | F0 曲线 + 音符基准线，可手绘
──────────┼──────────────────────────────
参数轨道  | 张力/力度/气息，可手绘
──────────┴──────────────────────────────
播放头 · 缩放 · 滚动 · 吸附
```

**它解决的问题**：
- 补上「MIDI / USTX 导入」——直接给现成旋律配音素
- 补上「逐音符 F0 编辑」——音符级滑音、颤音起点、装饰音、音准微调
- 歌唱模型/预测模型有了落地场景（否则 f0 是每音素一个定值，无处发挥）

**实现方式**：Canvas 自绘（已有 Charts 三个自定义 View 的经验）

## 010 · 自适应布局（16:9 / 横屏可用）
**状态**: 记录

**问题**：当前是单列纵向布局，固定高度约 **1844px**（实测 dump）：
```
标题行 122 · 副标题 80 · 音素输入 100
共振峰图 229 · 频谱图 204 · 音高图 178   ← 三张图共 611px
四个滑块 各约 132 = 528
状态栏 121
```

| 场景 | 可用高度 | 结果 |
|---|---|---|
| 本机 1080×2412（20:9）竖屏 | ~2158px | ✅ 放得下 |
| 16:9 竖屏 1080×1920 | ~1679px | ⚠️ 溢出 165px，要滚动 |
| **横屏 1920×1080** | **~969px** | ❌ **溢出 875px，几乎要用两屏** |

**改法**：自适应双栏
- **竖屏**：维持单列，图表高度改为弹性（最小 60dp）
- **横屏**：左右分栏 —— 左栏图表、右栏输入+滑块；图表压缩到 60dp
- 横竖屏都保证：**标题行按钮 + 状态栏始终可见**，不靠滚动
- 布局外置（004）后可热调，不用重装

**关联**：009 钢琴卷帘天然需要横屏；010 是它的前置条件。

## 011 · 横屏三区布局（卷帘 + 歌手 + 轨道）
**状态**: 记录
**前置**: 010（自适应布局）

### 1) 方向
- **解开竖屏锁定**，允许横屏

### 2) 横屏三区
```
┌────────┬──────────────────────────┬────────┐
│ 歌手区 │      主音符编辑区         │ 轨道区 │
│(侧边栏)│   （卷帘 / 音符网格）      │(侧边栏)│
└────────┴──────────────────────────┴────────┘
```
- **主音符编辑区**：中心，占主空间
- **歌手区**：侧边栏
- **轨道区**：侧边栏

### 3) 卷帘（照 OpenUtau）
- 音符卷帘 + **音符网格**
- **自定义配色**（可换主题色）

### 4) 轨道规则
- **轨道编号从 1 开始**：轨道一 = 第一个轨道
- **不存在「轨道 0 = 全部」这种概念**
- 后续轨道依次为二、三……

### 5) 歌手栏 / 声库
- **歌手栏不复用** —— 每个轨道独立歌手，不做共享/复用面板
- **点击头像 → 加载声库**（声库 = 音色参数文件 voices/*.json，对应 007）

### 6) 轨道系统
- 保留
- **轨道页面可「新建空白轨道」** —— 新轨道初始无歌手，需点头像加载声库

## 011 补充 · 声库 = zip（不是目录）
- **没有 C（目录型声库）**：A（音色参数）和 B（ONNX 模型）都存在，但不做成目录结构
- **一个声库 = 一个 zip 压缩包**（换壳，容器即 zip）
- **该 zip 仅限于「三个 ONNX」状态** —— 里面只装三个 .onnx
  （polish / speech_f0 / singing_f0，对应 006）
- **音色参数在 zip 内**，就是一个 JSON 配置文件

**声库 zip 结构**：
```
xxx.zip
├── voice.json        音色参数（共振峰/声门源/jitter/气息/颤音…）
├── polish.onnx       Q8
├── speech_f0.onnx    Q8
└── singing_f0.onnx   Q8
```

## 010/011 定稿 · 侧边栏两种朝向统一
**统一思路**：歌手区与轨道区**始终是侧边栏**，两种朝向都保留三区结构，
只改**侧边栏宽度与展开方式** —— 不另做一套竖屏布局。

**横屏**（模仿电脑）：
```
┌────────┬──────────────────────┬────────┐
│ 歌手区 │    主音符编辑区       │ 轨道区 │
│ 常驻展开│   （卷帘/音符网格）    │ 常驻展开│
└────────┴──────────────────────┴────────┘
```

**竖屏**：同样三区，侧边栏**收窄成图标条**，点击/侧滑展开为浮层
```
┌──┬─────────────────┬──┐
│歌│                 │轨│
│手│   主音符编辑区    │道│   ← 不挤压卷帘
│栏│                 │栏│
└──┴─────────────────┴──┘
```

**要点**：竖屏下侧边栏展开用**浮层（overlay）**，不推挤中间的卷帘 —— 这就是"啥不耽误"。

**共用**：一套控件，只有侧边栏宽度/展开方式随朝向变化。

## 012 · 顶部图表栏 + 精细设置栏
**状态**: 记录

### 顶部栏：图表
- **共振峰图、频谱图（及音高图）放到顶部栏**
- 横屏：顶部栏横向排列；竖屏：横向可滚动或压缩

### 精细设置栏
- **下拉选择参数**，选中后显示对应的具体数值/控件
- 首项举例：**气声量**
- 候选参数（引擎现有可调项）：
  | 参数 | 现状 |
  |---|---|
  | 气声量 / 气息感 | breath_ 已存在 |
  | jitter | 已存在（现写死 0.004） |
  | shimmer | 已存在（现写死 0.02） |
  | 声线缩放 | 已存在 |
  | 颤音深度 / 速率 / 起振延迟 | 已存在（现写死 0.010/5Hz/0.35） |
  | 鼻音耦合 | 已存在 |
  | 开商 / 张力（LF 参数） | **待暴露**（含 α 硬伤） |
  | 基频 F0 / 时长 | 已存在（当前在主滑块） |

**布局示意**：
```
┌──────────────────────────────────────┐
│ [共振峰图]  [频谱图]  [音高图]         │ ← 顶部图表栏
├────────┬──────────────────┬──────────┤
│ 歌手区 │   主音符编辑区    │ 轨道区    │
│        │    （卷帘）       │          │
├────────┴──────────────────┴──────────┤
│ 精细设置 ▾[气声量]  ●━━━━━━━  0.35    │ ← 精细设置栏
│          ▾[张力]    ●━━━━━    0.60    │
└──────────────────────────────────────┘
```

## 013 — 声道频响缺陷修复（v0.3.4-alpha）

### 实测定位（非推测）
用冲激响应 + 分频带能量测量，发现三处硬伤：

1. **5 节全极点级联的固有倾斜**：F1(800Hz) 峰值 +52.9 dB，F5(4950Hz) 仅 −10.9 dB，
   **F1→F5 落差 −64 dB**，且 5 kHz 以上以 −116 dB/oct 崩落，8 kHz 以上能量严格为 0。
   用 Klatt 本人的经典 /a/ 参数（730/1090/2440/3400/4400）验证，落差达 −75.8 dB，
   **证明这是级联结构的固有特性，不是实现 bug**。

2. **缺少并联支路**：Klatt 之所以自然，是因为他的合成器有一条并联支路
   （F1–F6 幅度显式给定）。我们只有级联，导致高频被反复衰减。

3. **元音间响度差 26.7 dB**（`u` 比 `i` 响 21.6 倍），且输出电平高达 +103 dBFS（写 WAV 必溢出）。

### 修复
- `KLTract` 新增 **Klatt 式并联支路**（F3/F4/F5，权重由自然包络反解 1 : 0.333 : 0.122），
  增益 `hfGain` 在 `process()` 时实时套用，默认 **0.16**。
- 并联支路与级联**共用同一个归一化源**（此前并联按未归一化的 `cascGain_`=131 dB 定标，被放大 9 个数量级）。
- `configure()` 内用冲激响应能量估计总增益 → **逐元音响度归一化**。
- `VoxEngine::render()` 末尾 **输出峰值归一化**至 0.85（可用 `setAutoNormalize(false)` 关闭）。

### 验证结果
| 指标 | 修复前 | 修复后 | 自然语音目标 |
|---|---|---|---|
| F1→F5 包络 | 0 / −9.5 / −28.6 / −40.6 / −63.9 dB | 0 / −9.5 / −13.3 / −24.1 / −39.7 dB | 0 / −5 / −15 / −25 / −35 dB |
| 元音响度动态范围 | 26.7 dB | **4.1 dB** | — |
| 输出峰值 | +103 dBFS（溢出） | **−1.4 dBFS** | — |

### 新增接口
`KLTract::setHfGain(double)` / `setTiltComp(double)` / `setRadiation(double)`；
`VoxEngine::setHfGain` / `setTiltComp` / `setRadiationFor` / `setAutoNormalize`。
